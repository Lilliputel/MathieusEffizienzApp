<UserControl x:Class="FrontLayer.Views.PlanView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 
			 xmlns:components="clr-namespace:FrontLayer.Components"
			 xmlns:classes="clr-namespace:ModelLayer.Classes;assembly=ModelLayer"
			 xmlns:converter="clr-namespace:FrontLayer.Converters"
			 xmlns:Utility="clr-namespace:ModelLayer.Utility;assembly=ModelLayer">

	<UserControl.Resources>
		<converter:ConverterColorToSCB x:Key="ColorConverter"/>
		<converter:DayTimeToBorderConverter x:Key="TimeToBorderConverter"/>
	</UserControl.Resources>

	<!-- Backgrond -->
	<Border Background="{DynamicResource Back_1}"
			Style="{StaticResource BorderRounded}">

		<ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">

			<ItemsControl ItemsSource="{Binding Categories}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<Grid/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate DataType="{x:Type classes:Category}">
						<components:WeekPlanView WeekPlan="{Binding WeekPlan}" Color="{Binding Color, Converter={StaticResource ColorConverter}}"
												 Height="400" Width="490"/>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>

		</ScrollViewer>
		
	</Border>
	
</UserControl>
 