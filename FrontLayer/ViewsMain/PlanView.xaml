<UserControl x:Class="FrontLayer.Views.PlanView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 
			 xmlns:components="clr-namespace:FrontLayer.Components"
			 xmlns:converter="clr-namespace:FrontLayer.Converters">

	<UserControl.Resources>
		<converter:ConverterColorToSCB x:Key="ColorConverter"/>
		<converter:DayTimeToBorderConverter x:Key="TimeToBorderConverter"/>
	</UserControl.Resources>

	<!-- Backgrond -->
	<Border Background="{DynamicResource Back_1}"
			Style="{StaticResource BorderRounded}">

		<ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
			<components:WeekPlanView WeekPlan="{Binding WeekPlan, UpdateSourceTrigger=PropertyChanged, BindsDirectlyToSource=True}" Height="400" Width="490"/>
		</ScrollViewer>
		
	</Border>
	
</UserControl>
 