<Window x:Class="FrontLayer.WPF.Windows.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:UserControls="clr-namespace:FrontLayer.WPF.Components"
		xmlns:DTSelector="clr-namespace:EffizienzControls.TemplateSelectors;assembly=EffizienzControls"
		Title="Prototype_Effizienz"
		WindowStartupLocation="CenterScreen"
		SizeToContent="WidthAndHeight"
		Background="{DynamicResource SecondaryBrush}">

	<Grid Margin="10">
		<!--#region Grid Definition -->
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<!-- #endregion -->

		<UserControls:Ribbon Grid.Column="1"
							 Grid.Row="0" />

		<UserControls:NavBar Grid.Column="0"
							 Grid.Row="1" />

		<!-- Hauptansicht -->
		<ContentControl Grid.Column="1"
						Grid.Row="1"
						Content="{Binding SelectedVMMain}"
						ContentTemplateSelector="{DTSelector:ViewModelSelector}"
						Margin="5"
						Width="860"
						Height="390" />

		<!-- Nebenansicht -->
		<ContentControl Grid.Column="2"
						Grid.Row="1"
						Content="{Binding SelectedVMEssential}"
						ContentTemplateSelector="{DTSelector:ViewModelSelector}"
						Margin="5"
						Width="220" />

	</Grid>

</Window>
