<UserControl x:Class="FrontLayer.WPF.Views.SettingsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
			 xmlns:global="clr-namespace:System.Globalization;assembly=mscorlib">

	<StackPanel>
		<ToggleButton Command="{Binding CommandChangeTheme}"
					  Content="{Binding ThemeButton, UpdateSourceTrigger=PropertyChanged}"
					  HorizontalAlignment="Stretch" VerticalAlignment="Center"
					  Margin="0,5"
					  Background="{DynamicResource SecondaryBrush}"/>
		<ToggleButton Command="{Binding CommandChangeCountDirection}"
					  Content="Change countdirection!"
					  HorizontalAlignment="Stretch" VerticalAlignment="Center"
					  Margin="0,5"
					  Background="{DynamicResource SecondaryBrush}"/>
		<ComboBox ItemsSource="{Binding Cultures}" x:Name="CultureComboBox"
				  SelectedItem="{Binding SelectedCulture}"
				  Margin="0,5">
			<ComboBox.ItemTemplate>
				<DataTemplate DataType="{x:Type global:CultureInfo}">
					<TextBlock Text="{Binding DisplayName}" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>
	</StackPanel>
	
</UserControl>
