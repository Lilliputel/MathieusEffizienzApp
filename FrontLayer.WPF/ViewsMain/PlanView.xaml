<UserControl x:Class="FrontLayer.WPF.Views.PlanView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 
			 xmlns:components="clr-namespace:FrontLayer.WPF.Components"
			 xmlns:converter="clr-namespace:FrontLayer.WPF.Converters"
			 xmlns:manager="clr-namespace:LogicLayer.Manager;assembly=LogicLayer"
			 DataContext="{x:Static manager:ViewModelManager.Plan}">

	<!-- Backgrond -->
	<Border Background="{DynamicResource Back_1}"
			Style="{StaticResource BorderRounded}">

		<ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" VerticalAlignment="Top">
			<components:WeekPlanView WeekPlan="{Binding WeekPlan, UpdateSourceTrigger=PropertyChanged, BindsDirectlyToSource=True}" 
									 Height="400" Width="490"/>
		</ScrollViewer>
		
	</Border>
	
</UserControl>
 